<div data-autocomplete-search-target = "list">
<% @sorted_misses.each do |miss| %>
  <ul>
    <%= miss.region %>
    <%= image_tag "#{miss.photo}.jpg"%>
    <%# <%= link_to "voir", year_miss_path(@year, miss) %>
    <%# <%= link_to "voir", year_miss_path(@year, miss), remote: true, data: { turbo_frame: "modal" } %>
    <%= link_to "voir", new_year_miss_category_path(@year, miss), remote: true, data: { turbo_frame: "modal" } %>
  </ul>
<% end %>
</div>

